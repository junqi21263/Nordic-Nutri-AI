import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lewislee.nordicnutri',
  appName: 'Nordic Nutri',
  webDir: 'mini-program/dist/h5',
};

export default config;
