# AI 餐食识别结果渐进呈现动效报告

## UI 冻结承诺

本次只新增 motion 状态和过渡效果，未改动分析结果页的布局、组件排序、图片、文字、间距、按钮、路由、接口、数据模型或保存逻辑。

## 触发与不触发

- 仅当扫描页收到真实 `analyzeProductImage` 结果、写入既有 analysis store 并跳转至分析结果页时播放一次。
- 触发标记在结果页首次挂载时立即消费；返回、刷新、历史餐次入口和直接进入不会播放。
- 跳转失败时会清除标记，避免下次误播。
- 动画不重新调用识别接口，也不创建、修改或保存餐食。

## 时序

| 时点 | 阶段 | 效果 |
| ---: | --- | --- |
| 0ms | imageReady | 原图 300ms 轻微上移、缩放并显现 |
| 300ms | status | AI 完成状态 260ms 显现 |
| 700ms | foodReveal | 真实食材行每项间隔 90ms 显现 |
| 1600ms | nutritionReveal | 评分与营养区 340ms 显现，数值保持 0 |
| 1900ms | nutritionCounting | 热量、宏量和进度条在 760ms 内到真实值，宏量间隔 60ms |
| 2700ms | actionReveal | 原有操作区 280ms 显现 |
| 3200ms | complete | 保持原有完整结果页 |

统一缓动：`cubic-bezier(.22, 1, .36, 1)`。

## 无障碍与开发调试

- `data-reduced-motion="true"` 时禁用本次新增的 opacity/transform 过渡；阶段顺序仍保留，以避免改变真实识别结果的数据呈现次序。
- 非生产构建提供“重播动画”入口，只复用当前已存在的真实结果，不产生网络请求；生产构建不显示。

## 自动验证

- `meal-recognition-motion.test.ts` 覆盖时序常量、动态食材 stagger、控制器取消行为、真实触发消费和开发重播边界。
- `ai-meal-flow.test.ts` 覆盖一次性触发标记的消费及 reset。
- `animated-progress.test.ts` 覆盖共享进度组件新增的可选受控显现能力。

自动测试、类型检查、lint 和构建只能证明代码边界与产物，不等同于真机视觉验收。

## 待人工 DevTools / 真机验收

使用真实拍照或相册识别完成一次流程，在以下时点与 Stitch Motion Golden Reference 比对：

- [ ] 0ms：原页面位置不跳动，原图尚未完全出现。
- [ ] 300ms：原图稳定，AI 完成状态开始显现。
- [ ] 700ms：识别食材按真实列表逐项出现。
- [ ] 1000ms：食材 stagger 不会因食材数量变化而重叠或遗漏。
- [ ] 1500ms：食材区保持稳定，营养区尚未开始计数。
- [ ] 2000ms：卡片已显现，热量/宏量从 0 向真实值递增。
- [ ] 2500ms：进度条接近真实目标，数值准确且无布局变化。
- [ ] 3000ms：保存与调整份量操作已显现。
- [ ] 返回、刷新、历史详情页：不应重播。
- [ ] 真机：保存本餐、调整份量、查看原图仍保持既有行为。
